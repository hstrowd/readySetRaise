Thanks for Your Support!
============================

Thanks so much for participating in the <%= @event.title %>, supporting our <%= @event.fundraiser.title %>. Your support is greatly valued and appreciated.

Below is a list of the pledges you made at this event:

<% @pledges.each do |pledge| %>
  - <%= humanize_time(pledge.created_at) %>: $<%= ('%.2f' % pledge.amount) %><% if pledge.monthly %> per month<% end %> for team <%= pledge.team.name %>
<% end %>

This brings <% if (@monthly_total > 0) && (@one_time_total > 0) %>your one-time total pledge amount to $<%= ('%.2f' % @one_time_total) %> and your monthly total pledge amount to $<%= ('%.2f' % @monthly_total) %><% elsif (@monthly_total > 0) %>your monthly total pledge amount to $<%= ('%.2f' % @monthly_total) %><% else %>your one-time total pledge amount to $<%= ('%.2f' % @one_time_total) %><% end %>. Please visit our donation page at <%= @event.organization.donation_url %> to complete this donation and fulfill your pledge.

Thanks again for joining us and supporting this great cause.